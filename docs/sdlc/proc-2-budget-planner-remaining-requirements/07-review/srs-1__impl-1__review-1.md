## Scope reviewed

**Prior-stage inputs read in full**: `srs-1/impl-1` (implementation code_change, 26 requirements + qa-1), `srs-1/impl-1/verify-1` (verification report).

**Graph tools run**: `detect_changes({scope:"all"})`; `impact({direction:"upstream", includeTests:true})` on `digitizeReceipt`, `InMemoryReceiptStore`, `ReceiptStore` (interface), `parseReceipt`, `OcrResult`, `StoredReceipt`; `explain()` (taint) on `app/api/receipts/route.ts` and `app/api/receipts/[id]/route.ts`. `git status --porcelain` run directly to cross-check `detect_changes` output (see Findings — it undercounts).

**Files read in full** (not excerpted): `lib/receipts/{types.ts,receiptStore.ts,digitizeReceipt.ts,parseReceipt.ts,ocrProvider.ts,validateFormat.ts,receiptStore.test.ts,digitizeReceipt.test.ts}`; `lib/{config.ts,errors.ts,singletons.ts}`; `lib/auth/currentUser.ts`; `lib/security/encryption.ts`; `lib/categorization/{categorizeReceipt.ts,categorizer.ts,categories.ts,correctionStore.ts,categorizeReceipt.test.ts}`; `lib/budget/{budgetService.ts,budgetService.test.ts}`; `lib/statistics/statisticsService.ts`; `lib/positionMatching/{positionMatching.ts,overrideStore.ts}`; `lib/goals/{goalStore.ts,adviceService.ts,goals.test.ts}`; `app/api/receipts/route.ts`; `app/api/receipts/[id]/route.ts` (+both `.test.ts`); `app/api/{budget,statistics}/route.ts` (+`.test.ts`); `app/api/{goals,advice,positions/compare,positions/override}/route.ts`; `app/page.tsx`; `app/receipts/{page.tsx,LineItemCategoryEditor.tsx}`; `app/budget/page.tsx`; `next-env.d.ts` diff; `vitest.config.ts`.

**Independently re-run, not just trusted**: `npm test` — 14 files, 109 tests, all passing (matches the verification report). `npm run build` — clean, all 11 routes compile (matches the verification report). Both run fresh in this stage, not copied from the prior stage's claims.

**Passes made** (sequential, each with a distinct lens):
1. **Correctness** — read every new/changed `lib/` module's logic against its cited requirement (categorization thresholds, budget month/exclusion logic, statistics ranking, position-matching exact-match rule, goal storage, advice threshold).
2. **Security** — reviewed the encryption and auth placeholders against their own doc comments; ran `explain()` taint analysis on the two receipt routes (0 findings — expected, no dangerous sinks exist in this codebase); checked ownership enforcement on every route that reads/writes a specific receipt by id; grepped for all callers of `assertOwnership`.
3. **Blast radius** — see the dedicated section below and the `dependents-outside-diff` answer.
4. **Coverage** — cross-checked the requirement/test table in `verify-1` against the actual test files, and separately enumerated every `app/api/**/route.ts` against every `app/api/**/*.test.ts` to find routes with no test at all.
5. **Adversarial** — deliberately tried to break `lib/categorization/correctionStore.ts`'s composite-key construction (a classic collision class), tried invalid/garbage input against `positions/override` and `/api/budget`'s query params, and checked whether the `globalThis`-singleton pattern could leak state across Vitest test files in a way that could mask a bug (it doesn't — Vitest's default per-file isolation gives each test file its own `globalThis`; only same-file test-order pollution is a codebase-wide plausible pattern, and existing tests use `.some()`/`.every()` assertions that are already robust to leftover shared-store data rather than fragile `.length` checks).

## Findings

**1. Test-coverage gap — 4 API routes have zero test coverage, not disclosed in `verify-1`'s Known Gaps. (MEDIUM, coverage, about the code but not a behavioral defect)**
`app/api/goals/route.ts`, `app/api/advice/route.ts`, `app/api/positions/compare/route.ts`, `app/api/positions/override/route.ts` — confirmed via `find app/api -name "*.test.ts"`, which returns only `budget`, `receipts`, `receipts/[id]`, and `statistics`. The `lib/` functions these routes call (`InMemoryGoalStore`, `getOptimizationAdvice`, `compareReceiptPhotos`, `resolvePairClassification`) are fully unit-tested, and I read all four route bodies and traced their logic — no bug found. But `verify-1`'s "Known gaps" section names only `app/receipts/page.tsx` and `app/budget/page.tsx` (REQ-29/30) as lacking automated coverage "verified only by manual browser testing" — these four routes have neither automated tests nor manual-browser coverage, since no UI currently calls them (position-matching and goals/advice are API-only in this slice, per the implementation artifact's own Risks section). This is a real, previously-unrecorded gap in what "Known gaps" claims to be complete.
Applies to: `srs-1/impl-1` and `srs-1/impl-1/verify-1` (the gap should have been named in verification's Known Gaps, and/or closed with route-level tests the way `app/api/statistics/route.test.ts` was added mid-verification for the same class of gap).

**2. Loose input validation on `app/api/positions/override/route.ts:7-8` (LOW, correctness, latent)**
```
const body = (await req.json()) as { pairId?: string; result?: PositionMatchResult };
if (!body.pairId || !body.result) {
```
`body.result` is only checked for truthiness, never validated against the two literal values of `PositionMatchResult` (`"same_position" | "different_position"`). `POST {pairId:"x", result:"bogus"}` succeeds today and stores `"bogus"` as if it were a valid classification. Low severity because nothing in the current codebase reads this value back through a code path that branches on it in a way that would be user-visible (position-matching isn't wired into any UI flow yet — a disclosed, separate gap). Worth tightening before a real caller is added.
Applies to: `srs-1/impl-1`.

**3. `assertOwnership` (`lib/auth/currentUser.ts:28`) is dead code, and the implementation artifact's requirement table overstates it (LOW, documentation/implementation mismatch, not a security defect)**
`srs-1/impl-1`'s requirement table states REQ-23/REQ-27 are "enforced in `app/api/receipts/[id]/route.ts`" via `assertOwnership`. Grepped the whole repo: `assertOwnership` has exactly one occurrence — its own definition. Every route that needs an ownership check (`GET`/`PATCH`/`DELETE` in `app/api/receipts/[id]/route.ts`) actually enforces it via `ReceiptStore.getForUser`'s inline `receipt.userId === userId` comparison, which is correct and covered by 4+ tests (`receiptStore.test.ts`, both `.../[id]/route.test.ts` cross-user tests). The real enforcement mechanism works; the artifact's description of *which* mechanism does the enforcing does not match the code. Risk is a future maintainer copying the documented-but-unwired `assertOwnership` pattern onto a new route and believing it does something it doesn't (it does throw `ForbiddenError` correctly if ever called — it's just never called).
Applies to: `srs-1/impl-1` (documentation accuracy only; no code change needed unless a maintainer wants `assertOwnership` actually used or removed).

**4. `next-env.d.ts` changed as a local-tooling side effect, not a hand-edit (INFO, no action needed)**
Two import paths changed (`./.next/dev/types/...` → `./.next/types/...`). This is Next.js's own auto-generated file (its header says "This file should not be edited"), regenerated by running `next dev`/`next build` locally. Confirmed harmless and unrelated to the feature; flagged only because an unrelated hunk in a diff is worth naming explicitly rather than silently passing through.

**Investigated and disproved (recorded so it isn't re-litigated)**: hypothesized `lib/categorization/correctionStore.ts`'s key-building (`merchant + KEY_SEPARATOR + itemName`) could collide when merchant/item names contain the separator (a classic bug class — e.g. "Fit"+"Mart Bar" vs "Fit Mart"+"Bar", both realistic given this codebase's own multi-word fixture names). Confirmed via `xxd` hex dump that `KEY_SEPARATOR` is an actual NUL byte, not a printable character as it first appeared through file-reading tooling (which rendered the NUL invisibly, making it look like a plain space). No defect.

## Blast radius

Every direct dependent found by `impact(direction:upstream, includeTests:true)` on the six most-consequential changed symbols was opened and read (full detail in the `dependents-outside-diff` answer; summarized here):

| Changed symbol | Direct dependents outside this change's own new/modified files | Status |
|---|---|---|
| `digitizeReceipt` (breaking signature change) | none outside the diff at depth 1 (sole caller `app/api/receipts/route.ts` was updated in the same change); depth-2 `app/api/receipts/route.test.ts` | Holds — opened, exercises new signature correctly |
| `InMemoryReceiptStore` / `ReceiptStore` (interface grew 5 methods; internal repr now encrypted) | `lib/categorization/categorizeReceipt.ts`, `lib/positionMatching/positionMatching.test.ts`, `lib/singletons.ts`, all 7 new API routes, both new pages | Holds — all consume the store through its public interface only; the encryption change (single `cipher` blob replacing plaintext fields) is fully encapsulated behind `get`/`all`/`save`/`update`, so no caller reaches into internal representation |
| `parseReceipt` / `OcrResult` (added optional `fieldConfidence`/`lowConfidenceFields`) | `lib/singletons.ts` (constructs a fixture `OcrResult` without `fieldConfidence`) | Holds — field is optional, `getLowConfidenceFields` treats a missing entry as full confidence |
| `StoredReceipt` (additive optional fields) | 13 direct dependents including **`lib/receipts/validateFormat.ts`, a proc-1 file this change never touches** | Holds — opened it directly; it imports only `SupportedFormat`, never references `StoredReceipt`/`LineItem`, provably unaffected. Every new consumer of the new optional fields uses `??` fallbacks, consistent with "additive only" |

No dependent outside the diff was found broken. The one file genuinely outside this change's own footprint that a naive diff-only review would likely have skipped — `validateFormat.ts` — was opened and confirmed unaffected rather than assumed.

**Graph-tool limitation found and worked around**: `detect_changes({scope:"all"})` reported only the 9 already-tracked modified files' symbol changes (`risk: "high"`, 38 symbols) and never surfaced any of the ~24 new/untracked files, because Meridian's index was built (during the verification stage) *after* those files already existed on disk — its git-diff-to-symbol mapping treats pre-existing-on-disk-but-uncommitted files as baseline, not as changes. Confirmed via `git status --porcelain`. This means `impact()`'s "outside the diff" dependents actually include files that `detect_changes` itself would never have flagged as part of the change (e.g. all 7 new API routes, both new pages) — I opened those by hand rather than relying on the tool to enumerate them. Recorded here as a process note: for a multi-file feature where nothing has been committed and the index was rebuilt mid-process, `detect_changes` cannot be trusted alone to define "the diff" — `git status`/`git diff` must be cross-checked.

## Residual risk

- **Prior behavior that might not hold**: none found. `validateFormat`/`UnsupportedFormatError` (proc-1) — unaffected, confirmed. The three proc-1 receipt-flow tests already in `digitizeReceipt.test.ts` before this change still pass with the new required `categorizer`/`corrections` parameters (both updated together in this diff, single caller).
- **Still unsure of, after looking**: whether the four untested routes (Finding 1) are correct under malformed/adversarial JSON bodies beyond what I traced by reading — I read the code and believe it's correct, but "read and believe correct" is exactly the gap a test closes and a review can't fully substitute for. Also unsure whether `app/api/positions/override`'s unchecked `result` string (Finding 2) will matter in practice, since I cannot see what a not-yet-written future caller will do with it.
- **Known, already-disclosed gaps re-confirmed, not new**: placeholder XOR encryption and two-hardcoded-user auth (both clearly documented in their own files, consistent with the concept-stage scope decision); REQ-29/REQ-30 UI screens have no automated test (only manual-browser, as stated); position-matching has no UI wiring yet (API-only). All confirmed accurate on inspection — none of these were found to be misrepresented.

## Verdict

**READY**, with two findings recorded for follow-up rather than blocking: the four-route test-coverage gap (Finding 1, the most substantive one) and the `assertOwnership` documentation/dead-code mismatch (Finding 3) should each get a short follow-up — closing the coverage gap the same way `verify-1` closed the statistics-route gap, and either wiring `assertOwnership` into a route or removing it — but neither is a behavioral defect in code that shipped: every requirement traces to real, correct, independently-verified code; every direct dependent outside the diff (including one file from an entirely different prior process) was opened and confirmed to still hold; the build and full test suite were independently re-run clean (109/109, build clean) rather than taken on the prior stage's word; and the one collision hypothesis serious enough to chase down (the correction-store key) was checked at the byte level and disproved. Finding 2 (loose validation on an unwired endpoint) is worth a one-line fix whenever that endpoint gets a real caller but does not block this stage on its own.
